import { MutableRefObject, useRef, useState } from "react";

interface SampleProps {
  title: string;
  children: React.ReactNode;
}

const handleButtonClick = () => {};

const Sample = () => {
  return (
    <div>
      <p>This is codign layer</p>
    </div>
  );
};

export default Sample;
